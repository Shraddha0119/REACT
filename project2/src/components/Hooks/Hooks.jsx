import React from "react";

const Hooks = ()=>{
    return(
        <div>
            <h1>
                Hooks are special function provides by react allow us to handle life cycle method and this.state.
            </h1>
            <h2>
                <li>use on the inside function components</li>
                 <li>use hooks at the  top level</li>
                 <li>hooks name must start with "use"</li>
            </h2>
            <p>usestate hook -manage data(state)</p>
            <p>useeffect hooks - side effect</p>
         
        </div>
    )
}

export default Hooks